# The knowledge base is its own git repo — never track it from the project repo.
/kb/

# Secrets / local environment — never commit
.env
.env.*
!.env.example

# Node
node_modules/
dist/
*.log

# Python — local virtualenv for tooling; never commit
.venv/
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
Thumbs.db

# Claude Code local config (settings, local skills) — machine-specific
.claude/
